# `@cursor/sdk` 1.0.31 audit

Package and contract check, 1.0.30 → 1.0.31. Not a live model receipt.

- Public delta schema and `coreToSdkUpdate` are byte-identical to 1.0.30. Still no event that marks the end of a model generation before local tools run. Upgrading does not change batch close; see [the batch-close probe](2026-09-04-tool-batch-close-live-probe.md).
- New: `AgentOptions.systemPrompt` (local, non-empty, account-gated; first `send` fails naming `--system-prompt`). Used by `HOST_SYSTEM_PROMPT_MODE=replace`.
- New: `SDKCustomTool.annotations`. Not mapped (no protocol IR carries MCP annotation hints).
- Cloud-only `agentServeAgent` / `SteerAckOutcome` unused.
- `Agent.create` / `resume` / `send`, `Run.stream` / `wait` / `cancel`, `Cursor.models.list`, and `Cursor.me` unchanged.
- Sand `x-cursor-client-type` sites unchanged in file and count; [`sand-patch-contract.ts`](../../src/sdk/sand-patch-contract.ts) pins the 1.0.31 hashes. Do not reuse 1.0.30 hashes.
